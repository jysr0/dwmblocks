#!/bin/bash

if [ "$(acpi -b | awk --field-separator=, '{ print $2 }' | tr -d ' ,%')" -lt 36 ]; then
	notify-send -u critical -t 3000 -a dunst '🔌 BATTERY LOW'
	#notify-send -a dunst 'BATTERY LOW'
fi

acpi -b | awk --field-separator=, '{ print $2 }' | tr -d ' '
